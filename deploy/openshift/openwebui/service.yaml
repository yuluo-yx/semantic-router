apiVersion: v1
kind: Service
metadata:
  name: openwebui
  namespace: vllm-semantic-router-system
  labels:
    app: openwebui
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: openwebui
